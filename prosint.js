switch (someVariable) {
  case 'functional':
    // Code to execute if someVariable === 'functional'
    break;
  case 'otherValue':
    // Code to execute if someVariable === 'otherValue'
    break;
  default:
    // Code to execute if someVariable doesn't match any case
}
